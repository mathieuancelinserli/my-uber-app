app {
  mode = "Dev"
  secret = "changeme"
  port = "9000"
  port = ${?PORT}
  session {
    # maxAge = 500
    secure = false
    httpOnly = false
    domain = "localhost"
    cookieName = "webstack-session"
  }
}
